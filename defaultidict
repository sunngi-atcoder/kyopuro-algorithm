from collections import defaultdict

# 初期値が10のdefaultdictを作成
initial_value = 10
d = defaultdict(lambda: initial_value)

print(d['a'])  # 出力: 10
print(d['b'])  # 出力: 10

#defaultdictを使うことによって馬鹿みたいに計算量かかる10**8ぐらいの大きさのリスト使わなくて済むので、使えるなら使った方が良い
